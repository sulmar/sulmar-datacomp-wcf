<Page
    x:Class="RestApiUWPClient.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestApiUWPClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <TextBox Header="Login" Text="{x:Bind ViewModel.Pracownik.Login}" />
            <TextBox Header="Hasło" Text="{x:Bind ViewModel.Pracownik.Password}" />

            <Button Click="{x:Bind ViewModel.Zaloguj}">Login</Button>


            <TextBlock Text="{x:Bind ViewModel.JestZalogowany}" />
            
            <!--<Ellipse x:Name="EllipseStatus" Width="20" Height="20" Fill="{x:Bind ViewModel.JestZalogowany, Converter=} />-->

        </StackPanel>
        
        
        
        
    </Grid>
</Page>
